# Domain docs

Single-context repo. Domain, rules, and stack are documented in `CONTEXT.md`
at repo root. No ADRs exist yet — add them under `docs/adr/` as
architectural decisions need recording (e.g. why no backend, why Vite over
Next.js).
